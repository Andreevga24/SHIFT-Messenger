# SHIFT Messenger

Мессенджер с открытым исходным кодом на Python с поддержкой реального времени.

История изменений по версиям: [CHANGELOG.md](CHANGELOG.md).

## Возможности

- ✅ Регистрация и аутентификация пользователей
- ✅ Обмен сообщениями в реальном времени (WebSocket)
- ✅ История сообщений
- ✅ Список пользователей, статусы и «видимый онлайн»
- ✅ Непрочитанные сообщения (счётчики в списке контактов)
- ✅ Графический интерфейс (PyQt5): пузыри чата, перенос длинных строк
- ✅ Консольный интерфейс
- ✅ Хранение данных в SQLite

## Структура проекта

```
SHIFT-Messenger/
├── server/
│   ├── __init__.py
│   └── server.py              # WebSocket сервер
├── client/
│   ├── __init__.py
│   ├── client.py              # Клиентский модуль
│   ├── gui.py                 # Графический интерфейс
│   └── console_client.py      # Консольный интерфейс
├── data/
│   └── shift.db               # База данных (создается автоматически)
├── statuses.py                # Допустимые статусы пользователя (общие для сервера и клиентов)
├── CHANGELOG.md               # История изменений по версиям
├── START.bat                  # Запуск сервера
├── CLIENT.bat                 # Запуск GUI клиента
├── CONSOLE.bat                # Запуск консольного клиента
├── run_server.py              # Скрипт запуска сервера
├── run_client.py              # Скрипт запуска GUI клиента
├── debug_client.py            # Диагностика клиента
├── requirements.txt           # Зависимости
├── README.md                  # Этот файл
└── ЗАПУСК.md                  # Подробная инструкция
```

## Быстрый запуск (Windows)

### Вариант 1: Консольный клиент (рекомендуется для тестирования)

1. **Запуск сервера**: Дважды кликните на `START.bat`
2. **Запуск клиента**: Дважды кликните на `CONSOLE.bat`

### Вариант 2: GUI клиент

1. **Запуск сервера**: Дважды кликните на `START.bat`
2. **Запуск клиента**: Дважды кликните на `CLIENT.bat`

### Вариант 3: Через командную строку

**Терминал 1 (сервер):**
```bash
python run_server.py
```

**Терминал 2 (клиент GUI):**
```bash
python run_client.py
```

**Или консольный клиент:**
```bash
python client/console_client.py
```

## Установка зависимостей

Если зависимости не установлены:
```bash
pip install -r requirements.txt
```

## Использование

### Консольный клиент

1. Запустите `CONSOLE.bat` или `python client/console_client.py`
2. Выберите "1. Регистрация" и создайте аккаунт
3. Выберите "2. Вход" для входа в систему
4. Выберите «1. Показать пользователей» — список, статусы и непрочитанные
5. Выберите «2. Выбрать чат», затем «3. Отправить сообщение» для переписки
6. При необходимости: «4. Обновить историю чата», «5. Установить статус»

### GUI клиент

1. Запустите `CLIENT.bat` или `python run_client.py`
2. Нажмите "Регистрация" и создайте аккаунт
3. Войдите в систему
4. При необходимости выберите **«Мой статус»** в выпадающем списке над контактами
5. Выберите пользователя из списка слева (видны статус и непрочитанные)
6. Начните общение: сообщения отображаются в виде пузырей; длинные строки без пробелов переносятся автоматически

## Запуск нескольких клиентов

Для тестирования чата запустите несколько клиентов:
- Дважды кликните на `CONSOLE.bat` или `CLIENT.bat` несколько раз
- Или запустите в разных терминалах

## Технологии

- **Python 3.8+**
- **websockets** - для обмена сообщениями в реальном времени
- **PyQt5** - для графического интерфейса
- **SQLite** - для хранения данных

## Подробная документация

Смотрите файл `ЗАПУСК.md` для подробной инструкции по использованию и устранению проблем.

## Лицензия

MIT License

## Автор

SHIFT Messenger Team